version: 2
updates:
  # Application dependencies are managed with Bun so every update can include
  # and validate the corresponding bun.lock. GitHub Actions remain safe for
  # Dependabot to update directly because they have no package-manager lockfile.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 3
